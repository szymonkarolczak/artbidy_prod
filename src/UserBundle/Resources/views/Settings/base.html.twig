{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <div class="container">

        <div class="page-header mt_0">
            <h1 class="mt_0">{{ 'profile.ustawienia.ustawienia'|trans }}</h1>
        </div>
        
        <div class="row">
            <div class="col-md-3">
                <ul class="nav nav-pills nav-stacked"> 
                    <li role="presentation"{% if app.request.attributes.get('_route') == 'profile_settings_general' %} class="active"{% endif %}><a href="{{ path("profile_settings_general") }}">{{ 'profile.ustawienia.ogolne'|trans }}</a></li> 
                    <li role="presentation"{% if app.request.attributes.get('_route') == 'profile_settings_changepass' %} class="active"{% endif %}><a href="{{ path("profile_settings_changepass") }}">{{ 'profile.ustawienia.zmiana_hasla'|trans }}</a></li>
                    {#<li role="presentation"{% if app.request.attributes.get('_route') == 'profile_settings_invoice' %} class="active"{% endif %}><a href="{{ path("profile_settings_invoice") }}">{{ 'profile.ustawienia.dane_do_faktury'|trans }}</a></li>#}
                </ul>
            </div>
            <div class="col-md-9">
                {% block settings %}
                {% endblock %}
            </div>
        </div>

    </div>
{% endblock %}
